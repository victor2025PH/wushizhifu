# 项目结构规划

## 📁 新的目录结构

### 本地和 GitHub 仓库结构

```
wushizhifu/
├── .env                    # 根目录，包含 BOT_TOKEN 和 BOT_TOKEN_B
├── .gitignore
├── .github/
│   └── workflows/
│       ├── deploy-botA.yml    # Bot A 自动部署
│       └── deploy-botB.yml    # Bot B 自动部署
├── botA/                   # Bot A (支付 Bot - 原 wushizhifu-bot)
│   ├── bot.py
│   ├── config.py
│   ├── database/
│   ├── handlers/
│   ├── keyboards/
│   ├── services/
│   ├── requirements.txt
│   └── ...
├── botB/                   # Bot B (OTC 群组管理 Bot - 原 wushizhifu-otc-bot)
│   ├── bot.py
│   ├── config.py
│   ├── database.py
│   ├── services/
│   ├── requirements.txt
│   └── ...
└── README.md
```

### 服务器部署结构

```
/home/ubuntu/wushizhifu/
├── .env                    # 包含 BOT_TOKEN 和 BOT_TOKEN_B
├── botA/
│   ├── venv/
│   ├── bot.py
│   ├── *.db
│   └── ...
└── botB/
    ├── venv/
    ├── bot.py
    ├── otc_bot.db
    └── ...
```

## 🔧 配置文件更新

### Bot A (config.py)
- 从 `../.env` 读取 `BOT_TOKEN`

### Bot B (config.py)
- 从 `../.env` 读取 `BOT_TOKEN_B`

## 🚀 GitHub Actions 部署流程

1. 代码推送到 GitHub
2. GitHub Actions 触发
3. SSH 连接到服务器
4. 拉取最新代码
5. 安装依赖
6. 重启服务

## 📝 .env 文件格式

```env
BOT_TOKEN=your_bot_a_token_here
BOT_TOKEN_B=your_bot_b_token_here
```

