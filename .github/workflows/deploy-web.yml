name: Deploy Web Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'web/**'
      - '.github/workflows/deploy-web.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Pull Code
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 300s
          script: |
            SERVER_PATH="${{ secrets.SERVER_PATH || '/home/ubuntu/wushizhifu' }}"
            cd ${SERVER_PATH} || exit 1
            if [ -d ".git" ]; then
              git stash || true
              git pull origin main || echo "⚠️ Git pull失败，继续部署"
            else
              echo "⚠️ 当前目录不是Git仓库，跳过git pull"
            fi
      
      - name: Build and Deploy
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 300s
          script: |
            SERVER_PATH="${{ secrets.SERVER_PATH || '/home/ubuntu/wushizhifu' }}"
            WEB_DIR="${SERVER_PATH}/web"
            WEB_DIST="${WEB_DIR}/dist"
            
            echo "📦 开始构建Web前端..."
            cd ${WEB_DIR} || exit 1
            
            # Check if Node.js is installed
            if ! command -v node &> /dev/null; then
              echo "⚠️ Node.js未安装，开始安装..."
              curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
              sudo apt install -y nodejs
            fi
            
            # Install dependencies
            echo "📥 安装依赖..."
            npm install --production=false || npm ci || exit 1
            
            # Fix permissions for dist directory before build
            echo "🔧 修复dist目录权限..."
            if [ -d "dist" ]; then
              sudo rm -rf dist || rm -rf dist || true
            fi
            mkdir -p dist
            chmod -R 755 dist || true
            
            # Build project (output will be in web/dist)
            echo "🔨 构建项目..."
            npm run build || exit 1
            
            # Check if dist directory was created
            if [ ! -d "dist" ]; then
              echo "❌ 构建失败: dist目录不存在"
              exit 1
            fi
            
            # Set proper permissions for dist directory
            echo "🔒 设置文件权限..."
            sudo chown -R www-data:www-data dist || true
            sudo chmod -R 755 dist || true
            
            # Update Nginx configuration for web domain
            NGINX_CONFIG="/etc/nginx/sites-available/web-5050"
            DOMAIN="5050.usdt2026.cc"
            
            echo "⚙️ 配置Nginx站点..."
            # Create Nginx configuration file
            cat > /tmp/web-5050-nginx.conf <<'NGINX_EOF'
# Nginx 配置文件 - Web前端网站
NGINX_EOF
            echo "# 域名: ${DOMAIN}" >> /tmp/web-5050-nginx.conf
            cat >> /tmp/web-5050-nginx.conf <<'NGINX_EOF'

server {
    listen 80;
    server_name DOMAIN_PLACEHOLDER;
    
    # 重定向到 HTTPS（Certbot 會自動添加）
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name DOMAIN_PLACEHOLDER;
    
    # SSL 證書路徑（Certbot 會自動配置）
    # ssl_certificate /etc/letsencrypt/live/DOMAIN_PLACEHOLDER/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/DOMAIN_PLACEHOLDER/privkey.pem;
    
    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;
    
    # Web前端靜態文件
    root WEB_DIST_PLACEHOLDER;
    index index.html;
    
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # 靜態資源緩存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Gzip 壓縮
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/json;
}
NGINX_EOF
            # Replace placeholders
            sed -i "s|DOMAIN_PLACEHOLDER|${DOMAIN}|g" /tmp/web-5050-nginx.conf
            sed -i "s|WEB_DIST_PLACEHOLDER|${WEB_DIST}|g" /tmp/web-5050-nginx.conf
            sudo mv /tmp/web-5050-nginx.conf ${NGINX_CONFIG}
            
            # Enable site
            sudo ln -sf ${NGINX_CONFIG} /etc/nginx/sites-enabled/
            
            # Check if SSL certificate exists
            if [ -f "/etc/letsencrypt/live/${DOMAIN}/fullchain.pem" ]; then
              echo "✅ SSL证书已存在，启用HTTPS配置..."
              sudo sed -i "s|# ssl_certificate|ssl_certificate|g" ${NGINX_CONFIG}
              sudo sed -i "s|# ssl_certificate_key|ssl_certificate_key|g" ${NGINX_CONFIG}
            else
              echo "⚠️ SSL证书不存在，先使用HTTP配置"
              echo "💡 请运行以下命令申请SSL证书："
              echo "   sudo certbot --nginx -d ${DOMAIN}"
            fi
            
            # Reload Nginx
            echo "🔄 重载Nginx..."
            sudo nginx -t && sudo systemctl reload nginx || echo "⚠️ Nginx重载失败"
            
            echo "✅ Web前端部署完成"
