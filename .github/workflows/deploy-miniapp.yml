name: Deploy Miniapp

on:
  push:
    branches:
      - main
    paths:
      - 'wushizhifu-full/**'
      - 'api_server.py'
      - '.github/workflows/deploy-miniapp.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Pull Code and Build
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 600s
          script: |
            cd /home/ubuntu/wushizhifu
            git stash || true
            git pull origin main
            
            # 进入 wushizhifu-full 目录
            if [ -d "repo/wushizhifu-full" ]; then
              cd repo/wushizhifu-full
            elif [ -d "wushizhifu-full" ]; then
              cd wushizhifu-full
            else
              echo "❌ 错误: 找不到 wushizhifu-full 目录"
              exit 1
            fi
            
            # 安装依赖
            echo "📦 安装依赖..."
            npm install --production=false || {
              echo "⚠️  npm install 失败，尝试清理后重试..."
              rm -rf node_modules package-lock.json
              npm install --production=false
            }
            
            # 构建项目
            echo "🔨 构建项目..."
            export NODE_OPTIONS="--max-old-space-size=3072"
            npm run build || {
              echo "❌ 构建失败"
              exit 1
            }
            
            # 检查构建结果
            if [ ! -d "dist" ]; then
              echo "❌ dist 目录不存在"
              exit 1
            fi
            
            echo "✅ 构建完成"
      
      - name: Deploy Frontend
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 300s
          script: |
            cd /home/ubuntu/wushizhifu
            
            # 确定源目录和目标目录
            if [ -d "repo/wushizhifu-full/dist" ]; then
              SOURCE_DIR="repo/wushizhifu-full/dist"
            elif [ -d "wushizhifu-full/dist" ]; then
              SOURCE_DIR="wushizhifu-full/dist"
            else
              echo "❌ 错误: 找不到构建后的 dist 目录"
              exit 1
            fi
            
            TARGET_DIR="$HOME/wushizhifu/frontend"
            
            # 备份现有目录
            if [ -d "$TARGET_DIR" ]; then
              echo "📦 备份现有目录..."
              mv "$TARGET_DIR" "${TARGET_DIR}.backup.$(date +%Y%m%d_%H%M%S)" || rm -rf "$TARGET_DIR"
            fi
            
            # 复制构建结果到 frontend/dist
            echo "📋 复制构建结果..."
            mkdir -p "$TARGET_DIR/dist"
            cp -r "$SOURCE_DIR"/* "$TARGET_DIR/dist/"
            echo "✅ 文件复制完成"
            
            # 设置文件权限
            echo "🔐 设置文件权限..."
            sudo chown -R www-data:www-data "$TARGET_DIR/dist" || sudo chown -R ubuntu:ubuntu "$TARGET_DIR/dist"
            sudo chmod -R 755 "$TARGET_DIR/dist"
            echo "✅ 权限设置完成"
            
            # 更新 Nginx 配置（如果需要）
            NGINX_CONFIG="/etc/nginx/sites-available/50zf.usdt2026.cc"
            if [ -f "$NGINX_CONFIG" ]; then
              echo "🔧 检查 Nginx 配置..."
              # 确保 root 路径正确
              if ! grep -q "root $TARGET_DIR/dist;" "$NGINX_CONFIG"; then
                echo "⚠️  Nginx 配置中的 root 路径可能需要更新"
                echo "   当前 root 路径: $(grep 'root ' "$NGINX_CONFIG" | head -1)"
                echo "   应该设置为: root $TARGET_DIR/dist;"
              fi
            fi
            
            # 重载 Nginx
            echo "🔄 重载 Nginx..."
            sudo nginx -t && sudo systemctl reload nginx || {
              echo "⚠️  Nginx 重载失败，请检查配置"
            }
            
            # 重启 API 服务（如果存在）
            if [ -f "/home/ubuntu/wushizhifu/api_server.py" ]; then
              echo "🔄 重启 API 服务..."
              sudo systemctl restart wushipay-api.service || true
            fi
            
            echo "✅ 部署完成"
            echo "🌐 访问: https://50zf.usdt2026.cc"

