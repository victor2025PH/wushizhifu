name: Deploy Miniapp

on:
  push:
    branches:
      - main
    paths:
      - 'wushizhifu-full/**'
      - 'api_server.py'
      - '.github/workflows/deploy-miniapp.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Pull Code and Build
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 600s
          script: |
            cd /home/ubuntu/wushizhifu
            git stash || true
            git pull origin main
            
            # 进入 wushizhifu-full 目录
            if [ -d "repo/wushizhifu-full" ]; then
              cd repo/wushizhifu-full
            elif [ -d "wushizhifu-full" ]; then
              cd wushizhifu-full
            else
              echo "❌ 错误: 找不到 wushizhifu-full 目录"
              exit 1
            fi
            
            # 安装依赖
            echo "📦 安装依赖..."
            npm install --production=false || {
              echo "⚠️  npm install 失败，尝试清理后重试..."
              rm -rf node_modules package-lock.json
              npm install --production=false
            }
            
            # 构建项目
            echo "🔨 构建项目..."
            export NODE_OPTIONS="--max-old-space-size=3072"
            npm run build || {
              echo "❌ 构建失败"
              exit 1
            }
            
            # 检查构建结果
            if [ ! -d "dist" ]; then
              echo "❌ dist 目录不存在"
              exit 1
            fi
            
            echo "✅ 构建完成"
      
      - name: Deploy Frontend
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: ${{ secrets.SSH_PORT || 22 }}
          timeout: 300s
          script: |
            cd /home/ubuntu/wushizhifu
            
            # 确定源目录和目标目录
            if [ -d "repo/wushizhifu-full/dist" ]; then
              SOURCE_DIR="repo/wushizhifu-full/dist"
            elif [ -d "wushizhifu-full/dist" ]; then
              SOURCE_DIR="wushizhifu-full/dist"
            else
              echo "❌ 错误: 找不到构建后的 dist 目录"
              exit 1
            fi
            
            TARGET_DIR="$HOME/wushizhifu/frontend/dist"
            
            # 使用 sudo 彻底删除旧文件（解决权限问题）
            if [ -d "$TARGET_DIR" ]; then
              echo "📦 删除旧部署文件..."
              sudo rm -rf "$TARGET_DIR"/*
              sudo rm -rf "$TARGET_DIR"/.[^.]* 2>/dev/null || true
              # 如果目录还存在，确保是空的
              if [ -d "$TARGET_DIR" ]; then
                REMAINING=$(find "$TARGET_DIR" -mindepth 1 2>/dev/null | wc -l)
                if [ "$REMAINING" -gt 0 ]; then
                  echo "⚠️  仍有文件残留，强制删除目录..."
                  sudo rm -rf "$TARGET_DIR"
                  sudo mkdir -p "$TARGET_DIR"
                fi
              else
                sudo mkdir -p "$TARGET_DIR"
              fi
              echo "✅ 旧文件已删除"
            else
              sudo mkdir -p "$TARGET_DIR"
            fi
            
            # 复制构建结果到 frontend/dist
            echo "📋 复制构建结果..."
            echo "源目录: $SOURCE_DIR"
            echo "目标目录: $TARGET_DIR"
            # 使用 sudo 复制以确保权限正确
            sudo cp -r "$SOURCE_DIR"/* "$TARGET_DIR"/
            sudo cp -r "$SOURCE_DIR"/.[^.]* "$TARGET_DIR"/ 2>/dev/null || true
            echo "✅ 文件复制完成"
            
            # 设置文件权限
            echo "🔐 设置文件权限..."
            sudo chown -R www-data:www-data "$TARGET_DIR"
            sudo chmod -R 755 "$TARGET_DIR"
            echo "✅ 权限设置完成"
            
            # 更新 Nginx 配置
            NGINX_CONFIG="/etc/nginx/sites-available/50zf.usdt2026.cc"
            if [ ! -f "$NGINX_CONFIG" ]; then
              # 如果配置文件不存在，尝试查找其他可能的配置文件名
              NGINX_CONFIG="/etc/nginx/sites-available/wushizhifu"
            fi
            
            if [ -f "$NGINX_CONFIG" ]; then
              echo "🔧 更新 Nginx 配置..."
              # 更新 root 路径
              sudo sed -i "s|root /.*/frontend/dist;|root $TARGET_DIR;|g" "$NGINX_CONFIG"
              sudo sed -i "s|root /.*/frontend;|root $TARGET_DIR;|g" "$NGINX_CONFIG"
              echo "✅ Nginx 配置已更新"
            else
              echo "⚠️  Nginx 配置文件不存在: $NGINX_CONFIG"
              echo "   请手动创建或检查配置文件位置"
            fi
            
            # 重载 Nginx
            echo "🔄 重载 Nginx..."
            sudo nginx -t && sudo systemctl reload nginx || {
              echo "⚠️  Nginx 重载失败，请检查配置"
            }
            
            # 启动/重启 API 服务
            echo "🔄 启动/重启 API 服务..."
            API_FILE="/home/ubuntu/wushizhifu/api_server.py"
            SERVICE_NAME="wushipay-api"
            
            if [ -f "$API_FILE" ]; then
              # 检查服务是否存在
              if [ ! -f "/etc/systemd/system/${SERVICE_NAME}.service" ]; then
                echo "创建 Systemd 服务..."
                PROJECT_ROOT="/home/ubuntu/wushizhifu"
                
                # 确定 Python 和 uvicorn 路径
                if [ -f "$PROJECT_ROOT/venv/bin/uvicorn" ]; then
                  UVICORN_CMD="$PROJECT_ROOT/venv/bin/uvicorn"
                elif [ -f "$PROJECT_ROOT/venv/bin/python3" ]; then
                  UVICORN_CMD="$PROJECT_ROOT/venv/bin/python3 -m uvicorn"
                else
                  UVICORN_CMD="python3 -m uvicorn"
                fi
                
                # 创建 systemd 服务文件（使用 cat 避免 heredoc 在 YAML 中的问题）
                sudo bash -c "cat > /etc/systemd/system/${SERVICE_NAME}.service <<'EOFSERVICE'
[Unit]
Description=WuShiPay API Service (FastAPI)
After=network.target

[Service]
Type=simple
User=ubuntu
Group=ubuntu
WorkingDirectory=$PROJECT_ROOT
Environment=\"PATH=$PROJECT_ROOT/venv/bin:/usr/local/bin:/usr/bin:/bin\"
Environment=\"PYTHONUNBUFFERED=1\"
Environment=\"PYTHONPATH=$PROJECT_ROOT\"
EnvironmentFile=-$PROJECT_ROOT/.env

ExecStart=$UVICORN_CMD api_server:app --host 127.0.0.1 --port 8000

Restart=always
RestartSec=10

StandardOutput=journal
StandardError=journal
SyslogIdentifier=wushipay-api

NoNewPrivileges=true
PrivateTmp=true
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
EOFSERVICE"
                sudo systemctl daemon-reload
                echo "✅ Systemd 服务已创建"
              fi
              
              # 安装 Python 依赖（如果需要）
              if [ -f "$PROJECT_ROOT/requirements.txt" ]; then
                if [ -f "$PROJECT_ROOT/venv/bin/pip" ]; then
                  $PROJECT_ROOT/venv/bin/pip install -q fastapi uvicorn python-multipart httpx requests || true
                else
                  pip3 install --user -q fastapi uvicorn python-multipart httpx requests || true
                fi
              fi
              
              # 停止占用端口的进程
              if lsof -i:8000 > /dev/null 2>&1; then
                sudo kill -9 $(lsof -t -i:8000) 2>/dev/null || true
                sleep 1
              fi
              
              # 启用并启动服务
              sudo systemctl enable ${SERVICE_NAME} || true
              sudo systemctl restart ${SERVICE_NAME} || sudo systemctl start ${SERVICE_NAME}
              
              sleep 2
              
              # 验证服务
              if systemctl is-active --quiet ${SERVICE_NAME}; then
                echo "✅ API 服务已启动"
              else
                echo "⚠️  API 服务启动失败，查看日志:"
                sudo journalctl -u ${SERVICE_NAME} -n 10 --no-pager || true
              fi
            else
              echo "⚠️  API 文件不存在，跳过 API 服务启动"
            fi
            
            echo "✅ 部署完成"
            echo "🌐 访问: https://50zf.usdt2026.cc"

