# Bot B 日志查看命令

## 📋 实时查看日志

### 方法一：使用 journalctl（推荐）

```bash
# 实时查看 Bot B 日志（跟随最新输出）
sudo journalctl -u otc-bot -f

# 查看最近 100 行日志
sudo journalctl -u otc-bot -n 100

# 查看最近 1 小时的日志
sudo journalctl -u otc-bot --since "1 hour ago"

# 查看今天的日志
sudo journalctl -u otc-bot --since today

# 查看错误日志
sudo journalctl -u otc-bot -p err

# 查看警告及以上级别日志
sudo journalctl -u otc-bot -p warning
```

### 方法二：查看最近日志（不跟随）

```bash
# 查看最近 50 行日志
sudo journalctl -u otc-bot -n 50

# 查看最近 200 行日志并显示时间戳
sudo journalctl -u otc-bot -n 200 --no-pager
```

### 方法三：查看服务状态

```bash
# 查看服务状态
sudo systemctl status otc-bot

# 查看服务是否运行
sudo systemctl is-active otc-bot

# 重启服务
sudo systemctl restart otc-bot

# 停止服务
sudo systemctl stop otc-bot

# 启动服务
sudo systemctl start otc-bot
```

## 🔍 常用日志筛选命令

```bash
# 只查看包含 "P2P" 的日志
sudo journalctl -u otc-bot -f | grep -i p2p

# 只查看包含 "error" 的日志
sudo journalctl -u otc-bot -f | grep -i error

# 查看特定用户的交互日志（替换 USER_ID）
sudo journalctl -u otc-bot -f | grep "USER_ID"

# 查看价格查询相关的日志
sudo journalctl -u otc-bot -f | grep -i price
```

## 📝 日志文件位置（如果有自定义日志文件）

如果 Bot B 有自定义日志文件，可能位于：
- `/home/ubuntu/wushizhifu/botB/logs/`
- `/home/ubuntu/wushizhifu/botB/bot.log`

使用以下命令查看：
```bash
# 实时查看日志文件
tail -f /home/ubuntu/wushizhifu/botB/logs/bot.log

# 或
tail -f /home/ubuntu/wushizhifu/botB/bot.log
```

## 🚀 快速测试命令组合

```bash
# 1. 检查服务状态
sudo systemctl status otc-bot

# 2. 实时查看日志（在另一个终端）
sudo journalctl -u otc-bot -f

# 3. 如果需要重启服务
sudo systemctl restart otc-bot
```

## 💡 提示

- 使用 `-f` 参数可以实时跟随日志输出（类似 `tail -f`）
- 使用 `Ctrl+C` 退出实时日志查看
- 如果日志太多，可以使用 `-n` 参数限制行数
- 使用 `--since` 和 `--until` 可以按时间范围筛选日志

